<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
    <t t-name="odoo-mercadolibre.instance_selector_template">
        <!-- <div class="o_menu_item">
            <select class="form-control"
                    t-on-change="onInstanceChange">
                <option value="all">ALL Instances</option>
                <t t-foreach="state.instances" t-as="instance" t-key="instance.id">
                    <option t-att-value="instance.id"><t t-esc="instance.name"/></option>
                </t>
            </select>
        </div> -->
        <select class="form-select" t-on-change="onInstanceChange">
        <t t-foreach="state.instances" t-as="instance" t-key="instance.id">
            <option t-att-value="instance.id" t-esc="instance.name"></option>
        </t>
    </select>
    </t>
</templates>
